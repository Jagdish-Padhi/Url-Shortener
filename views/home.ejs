<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HomePage </title>

    <link rel="stylesheet" href="/styles/home.css">


    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Iceland&family=Kanit:wght@500&family=Lato:ital,wght@0,100;0,300;0,400;0,700;0,900;1,100;1,300;1,400;1,700;1,900&family=Merriweather:ital,wght@0,300;0,400;0,700;0,900;1,300;1,400;1,700;1,900&family=Nerko+One&family=Nosifer&family=Playfair+Display:ital,wght@0,400..900;1,400..900&family=Poetsen+One&family=Prosto+One&family=Protest+Strike&family=Reggae+One&family=Trade+Winds&display=swap"
        rel="stylesheet">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>

<body>

    <div id="logo">
        <img src="/images/UrlShortLogo.webp" alt="ShortURL Logo" class="logo-img">
        <p class="logo-text">ShortURL</p>
    </div>

    <header>
        <div class="container">
            <% if (locals.id) { %>
                <div class="url-section">
                    <p>URL Generated: <a href="http://localhost:8001/url/<%= id %>" target="_blank"></a>
                        <%= id %>
                            </a>
                    </p>
                </div>
                <% } %>

                    <form method="POST" action="/url">
                        <label for="url">Enter Original URL</label>
                        <input type="text" id="url" name="url" placeholder="https://example.com" required />
                        <button type="submit">Generate</button>
                    </form>
        </div>
    </header>



    <% if (locals.urls) { %>

        <table>
            <thead>
                <tr>
                    <th>Sr.No.</th>
                    <th>Shorten URL</th>
                    <th>Original URL</th>
                    <th>Clicks</th>
                </tr>
            </thead>
            <tbody>
                <% urls.forEach((url, index)=> { %>
                    <tr>
                        <td>
                            <%= index + 1 %>
                        </td>
                        <td>
                            <a href="http://localhost:8001/url/<%= url.shortId %>">
                                <%= url.shortId %>
                            </a>

                        <td>
                            <%= url.redirectUrl %>
                        </td>
                        <td>
                            <%= url.visitHistory.length %>
                    </tr>

                    <% }) %>

            </tbody>
        </table>
        <% } %>
            

</body>

</html>